# ==========================================
# PART 1: SECRET (Locked Drawer 🔒)
# ==========================================
apiVersion: v1
kind: Secret
metadata:
  name: nodejs-secrets
type: Opaque  # "Opaque" ka matlab hai: K8s ko iske andar kya hai, usse fark nahi padta
stringData:   # 💡 Pro Tip: 'stringData' use karo, K8s khud Base64 mein convert kar dega!
  MONGO_URI: "mongodb+srv://t4technoshivam_db_user:f7ACSIcRwCQEoUeL@nodeapp.98rm2vy.mongodb.net/" 
  # Note: Agar tum 'data' use karte, toh upar wala base64 code paste karna padta. 
  # 'stringData' se tum direct plain text likh sakte ho, K8s background mein encode kar dega.

---
# ==========================================
# PART 2: CONFIGMAP (Normal Settings 📝)
# ==========================================
apiVersion: v1
kind: ConfigMap
metadata:
  name: nodejs-config
data:
  NODE_ENV: "production"
  PORT: "3000"